#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage('Debug') {
            steps {
                script {
                    echo "Back to basics."
                }
            }
        }
    }
}
